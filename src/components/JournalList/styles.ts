import styled from 'styled-components'


export const JournalList = styled.div<Props>`
    display: flex;
    flex-direction: row;
    margin-top: 40px;
    gap: 30px;
    flex-wrap: wrap;
    justify-content: space-evenly;
`